# Cloudflare Pages Configuration for dx.sh
# SvelteKit application with Cloudflare adapter

name = "devcrax"
pages_build_output_dir = ".svelte-kit/output/client"
compatibility_date = "2024-01-01"

# Optional: Add GitHub token for higher API rate limits
# Unauthenticated: 60 requests/hour
# Authenticated: 5000 requests/hour
# Set via Cloudflare Dashboard: Settings → Environment variables
# Or use: wrangler pages secret put GITHUB_TOKEN --project-name devcrax
