# ============================================
# dx.sh Environment Variables Configuration
# ============================================

# GitHub API Token (Optional but Recommended)
# --------------------------------------------
# Used by the Cloudflare Worker to fetch release information from GitHub
# Without token: 60 requests/hour per IP
# With token: 5000 requests/hour
# 
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Generate new token (classic)
# 3. No special scopes needed for public repositories
# 4. Copy the token and paste below
#
# For production deployment, set via Wrangler:
# wrangler secret put GITHUB_TOKEN --env production
GITHUB_TOKEN=

# Worker URL Configuration
# ------------------------
# This is the URL where the Cloudflare Worker is deployed
# The SvelteKit frontend uses this to fetch generated scripts
#
# Development: http://localhost:8787
# Production: https://dx.sh (or your custom domain/worker URL)
PUBLIC_WORKER_URL=http://localhost:8787

# Cloudflare Configuration (for deployment)
# ------------------------------------------
# These are typically set via Wrangler CLI or Cloudflare dashboard
# Not needed in .env file, but documented here for reference
#
# CLOUDFLARE_ACCOUNT_ID=your_account_id
# CLOUDFLARE_API_TOKEN=your_api_token
# CLOUDFLARE_ZONE_ID=your_zone_id (for custom domain routing)
